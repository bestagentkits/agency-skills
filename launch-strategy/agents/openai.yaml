interface:
  display_name: "Launch Strategy"
  short_description: "When the user wants to plan a product launch, feature..."
  default_prompt: "Use $launch-strategy to help with launch strategy work."
